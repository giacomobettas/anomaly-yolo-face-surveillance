# Data Folder

This folder will contain:
- `faces/` – known face images for identity recognition
- `camX/` – per-camera folders containing:
  - `normal_crops/` for autoencoder training
  - `videos/` (or user-provided paths)

Dataset is **NOT** included here.  
User must provide their own inputs.
